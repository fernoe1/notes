Continuation from the previous task.

In Leva:

Change position into a joystick where you can change both x and y values with a step of 0.01. Set joystick property to 'invertY'.

Add color property.

Add visible checkbox.

Create interval with min 0 max 0 values 4 and 5.

Create button using button from Leva.

Create choice options using options with array of possible values.

Use folders:

Put all properties of sphere inside sphere folder.

Create new cube folder with scale propety.

Import Leva in index.jsx.

Use Leva component to make it collapse at the start.