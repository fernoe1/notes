Continuation from the previous task.

Merge all non-emmision material objects into one, so you load them all in one go for optimization. Name the merged object 'baked'.

Instead of traversing the gltf.scene, get the baked object by using find method, and set its material to bakedMaterial.