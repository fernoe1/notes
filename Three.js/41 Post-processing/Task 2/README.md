Continuation.

Import and instantiate EffectComposer.

Import and instantiate a RenderPass with scene, camera parameters.

In the tick function do effectComposer.render() instead of renderer.render().

Import and instantiate a DotScreenPass and add it into effectComposer.