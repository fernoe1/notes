Load hit.mp3 from sounds directory.



Add collide event listener to both sphere and box that plays the hit.mp3 sound.



Using event object that's passed from collide event listener, make it so the sound is only played if impact is more than 1.5, and make sure the sound can be played multiple times in one frame.



Create reset function that removes every object from the scene both in three js and physical worlds. 



Add it to debug.



You must have CSS that makes your canvas take whole screen.



You must have resize event listener that listens to resizes.



You must have 'h' keypress event listener that hides/shows the GUI.

