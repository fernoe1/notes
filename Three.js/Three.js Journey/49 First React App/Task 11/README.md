Continuation from the previous task.

Using useEffect hooks, and localStorage make count stay between page reloads.

Use first useEffect hook with no dependencies to fetch count value from localStorage and set it.

Use second useEffect hook with count dependency to change the count value whenever it is changed.