Continuation from the previous task.

Using react.fragment (`<>`) create multiple clickers with one && operator.

Pass keyName and color properties to those clickers, and implement them.

Pass static keyName and random color using hsl.