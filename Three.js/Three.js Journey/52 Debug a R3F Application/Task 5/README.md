Continuation from the previous task.

Install r3f-perf@6.5 using npm.

Import Perf component inside Experience.jsx.

Use it and set its position property to top-left so it doesn't conflict with Leva.

Create perfVisible checkbox which controls the visibility of Perf component. (Hint: use &&)