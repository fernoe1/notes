Continuation from the previous task.

Create ambientLight function that accept vec3 lightColor, float lightIntensity and returns vec3.

Use includes to put ambientLight inside another file and fetch it inside fragment.glsl and use it.