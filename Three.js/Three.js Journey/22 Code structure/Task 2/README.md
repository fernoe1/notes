Create robot class in a separate Robot.js file.

It must have:



Constructor that takes two parameters, name and legs adds them to context, console logs `I am ${this.name}. Thank you creator.`, and fires sayHi function.



sayHi function that introduces the robot.



Create flying robot class in a separate FlyingRobot.js file that extends from robot, adds two new functions takeoff and land, and overrides sayHi and constructor.



In script.js, import those classes and test them out.

