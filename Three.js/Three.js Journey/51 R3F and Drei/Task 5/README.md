Continuation from the previous task.

Import Text from @react-three/drei.

Text component uses SDF (signed distance fields) under the hood.

Set the text font to the one inside public folder.

Set the size to 1.

Set the color to salmon.

Set the position to 0, 2, 0.

Set the max width to 2.

Align the text to the center.