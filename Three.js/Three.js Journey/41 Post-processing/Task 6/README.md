Continuation.

Import and instantiate unrealBloomPass.
Put some values for unrealBloomPass.strength/radius/threshold.

Using gui, add enabled tweak for all passes.

Using gui, add strength, radius and threshold sliders to unrealBloomPass.

Using gui, add goWild tweak for glitchPass.