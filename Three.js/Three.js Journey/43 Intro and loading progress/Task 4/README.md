Continuation.

Inside index.html create loading bar that you will animate. 

Inside style.css style that and give transform attribute an animation.

Inside loading manager on progress callback, calculate progressRatio and set transform attribute of the bar using that.

on finish call back, fire an animation that makes the bar slide to the right side of the screen.