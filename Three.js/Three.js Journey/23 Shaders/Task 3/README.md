Continuation from the previous task.



Divide modelMatrix, viewMatrix, projectionMatrix multiplications into their own separate lines by creating new variables such as vec4 modelPosition, vec4 viewPosition, vec4 projectedPosition for more control.



Using modelPosition, change the position of z axis of the each vertex creating wave like effect.



(Hint: use sin() based on x).

