Continuation from the previous task.

Create uProgress uniform attribute, set it to 0.

Animate uProgress uniform attribute from 0 to 1 in 3 secs with linear easing using gsap. When animation is finished fire function that disposes of the fireworks.

Vertex shader:

Create exploding progress that makes particles explode from the center to their positions.

Create falling progress that makes particles fall.

Create sizeProgress that makes particles scale up then scale down.

Create twinkling progress that makes particels twinkle.

Handle gl_PointSize < 1.0 for windows users.

Create createRandomFirework function that is a wrapper around createFirework, fire it whenever click function is fired.