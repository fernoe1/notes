Continuation from the previous task.

Remove all the previous code from app.

Create Clicker.jsx component and create a simple counter and a button that increments that counter.
Use useState hook.

Import and place that component inside app component.