`fun` is used to declare a function.

`main()` function is where your program starts from.

Body of function is writed within `{}`

`println()` and `print()` write to stdout, one with linebreak one without.