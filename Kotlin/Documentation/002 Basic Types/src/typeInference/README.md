Kotlin has type inference.

`customers` is assigned an `Int` value, from this Kotlin knows that the type of `customers` is of `Int` type.

> [!NOTE]
> `+=`, `-=`, `/=`, and `%=` are augmented assignment operators.