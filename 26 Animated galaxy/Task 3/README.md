Continuation from the previous task.

Pass color attribute to fragment shader using varying and use mix, black color, and strength to color points.

Create rotation animation using modelPosition, atan; length functions and angle offset. Use sin, cos and length as radius to do circular animation.

Create randoms attribute and pass it down as aRandoms to fix ribbon problem.