Continuation.

Install and import gsap from 'gsap'.

Add uAlpha uniform to overlayMaterial and animate it using gsap.

(Hint: to know when to fire the animation, create loadingManager and pass it to the loaders and listen to callbacks.)