Create a hamburger inside blender, and export it as static/models/hamburger.glb using DRACO compression.



The starter already has required code to import it, but if you want, you can clear it and rewrite to practice. 

