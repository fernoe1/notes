Continue from the starter or from vite created react app.

Inside index html, create a fragment root.

Inside fragment, use h1 with className for any message you want.
Inside fragment, below h1, create paragraph message with <br />.
Inside fragment, below p, create input with type checkbox and id the-checkbox.
Inside fragment, below input, create label with htmlFor for the the-checkbox.