Continuation from the previous task.

Create createFirework function with these parameters:

count,

position,

size,

texture,

radius,

color


count: should controls how many particles there are in the screen.

position: should control where the particles gonna originate from.

size: should control base size of the particles. (It should be a lil randomized)

texture: the texture that will be used for particles.

radius: since particles will be placed down along the sphere's surface area, we need radius.

color: the color of the particles.

Implement this function using THREE.js, vertex and fragment shaders.


Using viewPosition.z add depth to particles.

Create new uResolution and use it to control how particles should react to resizing.