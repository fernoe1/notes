Continuation from the previous task.

Create color debugger using lil-gui, and uniforms.

Create random2D function and import it using #include.

Using that function make modelPosition x and z axes move in -0.5 and 0.5 range randomly.

Use time so the effects move even when the objects stand still.

Make glitch effect go up from the bottom to the top using modelPosition.y.

Make the effect random by using 3 sine functions at the same time.

Make the function have more wait times and smoother using smoothstep.