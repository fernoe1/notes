Continuation.

Inside html add a point and inside css style it, and add required animations.

After that inside script.js, create points array that stores each points position (vector 3) and their respective html element.

Inside tick function using project function animate translation of those points so the 3D point position gets translated to 2D viewport position.