Create the following pattern below using vUv inside fragment.glsl.

Use mix with blackColor, uvColor and strength for the color.

Clamp the strength value to avoid interpolation.

![Pattern](pattern.png)